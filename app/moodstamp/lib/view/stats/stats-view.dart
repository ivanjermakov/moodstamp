import 'package:flutter/cupertino.dart';

class StatsView extends StatelessWidget {
  StatsView();

  @override
  Widget build(BuildContext context) {
    return CupertinoPageScaffold(
      child: Center(
        child: Text('henlo stats'),
      ),
    );
  }
}
